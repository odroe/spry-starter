name: spry_starter
description: Starter template for building APIs with the current Spry file-routing runtime.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  spry: ^8.5.0

dev_dependencies:
  http: ^1.6.0
  lints: ^6.1.0
  test: ^1.31.0

dependency_overrides:
  frontend_server_client: ^4.0.0
